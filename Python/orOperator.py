# Lets you do multiple comparisons in an if statement

if (nDollars > 4) or dateIsPaying:
    # can get an ice cream sundae

if (studyingHoursPerDay > 4) or payOffTeacherForGoodGrade:
    # you will do well in class

if (userCommand == 'q') or (userCommand == 'quit'):
    # quit the program

if (age > 65) or disabled:
    # can get government benefits