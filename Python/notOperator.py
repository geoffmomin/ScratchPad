# The not operator can be used to represent the opposite of what is shown

if not open:
    # closed

if not broken:
    # working

# A working example of the not operator:
if not(width == length):
    # not a square

# You can use it in an assignment:
alive = not dead

# You can use it to reverse the output of a function:
isOdd = not isEven(value)